# config/linkedin.cfg
# Credenciales de LinkedIn API para PUBLISH-SIM.cbl.
#
# En esta demo se dejan vacias A PROPOSITO (ver requirements.md, REQ-E1:
# "no se facilitaran credenciales de empresa ni se espera que uses una
# cuenta personal"). Mientras ACCESS_TOKEN este vacio o este archivo no
# exista, PUBLISH-SIM.cbl corre siempre en modo SIMULATED.
#
# Ver README.md, seccion "PUBLISH-SIM.cbl", para el detalle de que
# credenciales hacen falta y como obtenerlas para pasar a modo real.
CLIENT_ID=
CLIENT_SECRET=
ACCESS_TOKEN=
